on: [pull_request]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Clone the Project
        uses: actions/checkout@v4

      - name: Test Common Lisp
        id: foo
        uses: fosskers/cl-action@v0.0.3
        with:
          compiler: "sbcl"
